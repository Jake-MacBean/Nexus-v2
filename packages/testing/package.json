{
  "name": "@nexus-v2/testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false",
    "fixtures:test": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false && node --test dist/fixtures.test.js",
    "fixtures:test:integration": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false && node --env-file-if-exists=../../.env --test dist/postgres-fixture.integration.test.js",
    "typecheck": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@nexus-v2/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3"
  }
}
