{
  "name": "@nexus-v2/testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false",
    "fixtures:test": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false && vitest run --config ../../vitest.config.ts --project unit-node packages/testing/src/fixtures.test.ts",
    "fixtures:test:integration": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --pretty false && node --env-file-if-exists=../../.env ../../node_modules/vitest/vitest.mjs run --config ../../vitest.config.ts --project integration-node packages/testing/src/postgres-fixture.integration.test.ts",
    "typecheck": "pnpm --filter @nexus-v2/database build && tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@nexus-v2/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vitest": "catalog:"
  }
}
