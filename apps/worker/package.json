{
  "name": "@nexus-v2/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --pretty false",
    "start": "node dist/index.js",
    "typecheck": "pnpm --filter @nexus-v2/observability build && tsc -p tsconfig.json --noEmit --pretty false"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@nexus-v2/observability": "workspace:*"
  }
}
