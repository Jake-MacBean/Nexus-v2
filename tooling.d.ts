/**
 * T01 compiler sentinel.
 *
 * The package graph is intentionally empty until the bounded package-scaffolding
 * work packet. Keeping one root declaration makes the strict compiler gate real
 * without introducing business or runtime implementation.
 */
export {};
